kind: Service
apiVersion: v1
metadata:
  name: springboottest-service
spec:
  type: NodePort
  selector:
    app: springboottest
  ports:
  - name: springboottest-service
    protocol: TCP
    port: 80
    targetPort: 8080
